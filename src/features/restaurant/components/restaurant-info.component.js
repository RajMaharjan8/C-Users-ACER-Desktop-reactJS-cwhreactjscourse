import React from "react";
import { Text, StyleSheet, View } from "react-native";

export const RestaurantInfo = () => {
  return (
    <>
      <Text>Restauant Info will be made here</Text>
    </>
  );
};

const styles = StyleSheet.create({});
